apiVersion: v1
kind: Namespace
metadata:
  name: chainguard-app
  labels:
    app.kubernetes.io/managed-by: argocd

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: razz-bug-calenderapp
  namespace: chainguard-app
  labels:
    app: razz-bug-calenderapp
    app.kubernetes.io/managed-by: argocd
spec:
  replicas: 1
  selector:
    matchLabels:
      app: razz-bug-calenderapp
  template:
    metadata:
      labels:
        app: razz-bug-calenderapp
    spec:
      containers:
        - name: razz-bug-calenderapp
          # CI job 6 (update-manifest) pins this to an exact digest after every build.
          # ArgoCD syncs when this value changes.
          # The digest matches exactly what was signed, scanned, and attested in CI.
          image: ghcr.io/tripleaze/chainguard@sha256:fe2ac7dadc7530e8b252f2a1f53495b93816b5d122ac60af7994a57d9d05beae
          ports:
            - containerPort: 8080
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
