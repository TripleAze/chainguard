apiVersion: v1
kind: Namespace
metadata:
  name: chainguard-app
  labels:
    app.kubernetes.io/managed-by: argocd

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: razz-bug-calenderapp
  namespace: chainguard-app
  labels:
    app: razz-bug-calenderapp
    app.kubernetes.io/managed-by: argocd
spec:
  replicas: 1
  selector:
    matchLabels:
      app: razz-bug-calenderapp
  template:
    metadata:
      labels:
        app: razz-bug-calenderapp
    spec:
      containers:
        - name: razz-bug-calenderapp
          # CI job 6 (update-manifest) pins this to an exact digest after every build.
          # ArgoCD syncs when this value changes.
          # The digest matches exactly what was signed, scanned, and attested in CI.
          image: ghcr.io/tripleaze/chainguard@sha256:8f3e9a934c1f2c32a907f31d7c2f5c74cfdfe52a36de1878347d76522fccfe8d
          ports:
            - containerPort: 8080
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
